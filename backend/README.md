# Backend

This directory holds the server‑side code for the ShadowSpeak application, such as API endpoints, business logic, and integration with the chosen backend services (e.g., AWS serverless or Firebase).